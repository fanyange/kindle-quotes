= form_for @book, html: { class: "ui form" } do |f|
  .two.fields
    .field
      = f.collection_select(:id, Book.all, :id, :title, {selected: nil, prompt: "Select A Book To Show Its Quotes"}, class: "ui search dropdown")
