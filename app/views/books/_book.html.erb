<%= content_tag_for(:tr, book) do %>
  <td><%= link_to image_tag(book.image_url), book.douban_url%></td>
  <td><%= book.title %></td>
  <td><%= book.author %></td>
  <td><%= link_to book.quotes.size, book_quotes_path(book) %></td>
<% end %>
